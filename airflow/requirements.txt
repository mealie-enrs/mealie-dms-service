# Extra dependencies installed into the Airflow image at container startup.
# These are NOT part of the main DMS app image.
# NOTE: pyiceberg is intentionally excluded — it requires sqlalchemy>=2.0 which
# conflicts with Airflow 2.9's sqlalchemy<2.0 requirement.
# Airflow DAGs read from Postgres directly via psycopg2 instead.
confluent-kafka==2.4.0
redis==5.0.8
psycopg2-binary==2.9.9
