# Extra dependencies installed into the Airflow image at container startup.
# These are NOT part of the main DMS app image.
confluent-kafka==2.4.0
redis==5.0.8
pyiceberg[s3,pyarrow,sql-postgres]==0.7.1
psycopg2-binary==2.9.9
# Pin SQLAlchemy below 2.0 — Airflow 2.9 requires 1.4.x and pyiceberg[sql-postgres]
# would otherwise pull in 2.x which breaks Airflow's internal ORM.
sqlalchemy>=1.4,<2.0
